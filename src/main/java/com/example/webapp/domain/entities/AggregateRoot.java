package com.example.webapp.domain.entities;

/**
 * Marker interface to designate an Aggregate Root in the domain model.
 * Use this on entities that are entry points for invariants and persistence.
 */
public interface AggregateRoot {
}
